#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
int main(){
  char* line = NULL;
  size_t size = 0;
  printf("Please enter some text: ");
  ssize_t num_char = getline(&line, &size, stdin);
  
  if(num_char != 0){
  printf("buffer %s\n", line);
  }
  else{
  printf("Error");
  }

  free(line);
  return 0;


}
